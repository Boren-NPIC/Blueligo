"use client";
import { FormEvent, useState } from "react";
import { createSupabaseBrowser } from "@/lib/supabase/client";
export default function LoginPage() { const supabase = createSupabaseBrowser(); const [signup,setSignup]=useState(false); const [message,setMessage]=useState(""); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const email=String(fd.get("email"));const password=String(fd.get("password"));const result=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});if(result.error)setMessage(result.error.message);else if(signup)setMessage("សូមពិនិត្យ Email ដើម្បីបញ្ជាក់គណនី");else location.href="/";} async function google(){await supabase.auth.signInWithOAuth({provider:"google",options:{redirectTo:`${location.origin}/auth/callback`}})} return <main className="grid min-h-screen place-items-center bg-blue-50 p-5"><div className="w-full max-w-md rounded-3xl bg-white p-7 shadow-xl"><h1 className="text-3xl font-black text-blue-950">{signup?"បង្កើតគណនី":"ចូលគណនី"}</h1><button onClick={google} className="mt-6 w-full rounded-xl border border-blue-200 px-4 py-3 font-bold">បន្តជាមួយ Google</button><div className="my-5 text-center text-sm text-slate-400">ឬ</div><form onSubmit={submit} className="space-y-4"><input name="email" type="email" required placeholder="Email" className="w-full rounded-xl border p-3"/><input name="password" type="password" required minLength={6} placeholder="Password" className="w-full rounded-xl border p-3"/><button className="w-full rounded-xl bg-blue-600 p-3 font-bold text-white">{signup?"Register":"Login"}</button></form>{message&&<p className="mt-4 text-sm text-red-600">{message}</p>}<button onClick={()=>setSignup(!signup)} className="mt-5 w-full text-sm font-bold text-blue-700">{signup?"មានគណនីរួច? ចូលគណនី":"មិនទាន់មានគណនី? Register"}</button></div></main> }
