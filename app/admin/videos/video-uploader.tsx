"use client";import Script from "next/script";import {useState} from "react";
declare global{interface Window{cloudinary?:{createUploadWidget:(options:Record<string,unknown>,callback:(error:unknown,result:{event?:string,info?:{secure_url?:string,duration?:number}})=>void)=>{open:()=>void}}}}
export default function VideoUploader(){const [url,setUrl]=useState("");const [title,setTitle]=useState("");const [courseId,setCourseId]=useState("english-starter");const [message,setMessage]=useState("");function upload(){const widget=window.cloudinary?.createUploadWidget({cloudName:process.env.NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME,uploadPreset:process.env.NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET,resourceType:"video",sources:["local","url"],multiple:false,folder:"bluelingo/lessons"},(_e,r)=>{if(r.event==="success"&&r.info?.secure_url)setUrl(r.info.secure_url)});widget?.open()}async function save(){const r=await fetch("/api/admin/lessons",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({courseId,title,videoUrl:url})});setMessage(r.ok?"បានរក្សាទុកមេរៀនរួចហើយ":"មិនអាចរក្សាទុកបានទេ")}return <main className="min-h-screen bg-blue-50 p-5"><Script src="https://upload-widget.cloudinary.com/latest/global/all.js"/><div className="mx-auto max-w-2xl rounded-3xl bg-white p-7 shadow-xl"><a href="/admin" className="font-bold text-blue-600">← Admin</a><h1 className="mt-5 text-3xl font-black text-blue-950">Upload វីដេអូមេរៀន</h1><div className="mt-6 space-y-4"><select value={courseId} onChange={e=>setCourseId(e.target.value)} className="w-full rounded-xl border p-3"><option value="english-starter">English Beginner</option><option value="english-work">English for Work</option><option value="chinese-starter">Chinese HSK 1</option><option value="chinese-business">Chinese Business</option></select><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="ចំណងជើងមេរៀន" className="w-full rounded-xl border p-3"/><button onClick={upload} className="w-full rounded-xl border-2 border-dashed border-blue-300 bg-blue-50 p-8 font-bold text-blue-700">ជ្រើសវីដេអូពីកុំព្យូទ័រ</button>{url&&<video src={url} controls className="w-full rounded-2xl"/>}<button disabled={!url||!title} onClick={save} className="w-full rounded-xl bg-blue-600 p-3 font-bold text-white disabled:opacity-50">Publish មេរៀន</button>{message&&<p className="text-center font-bold text-blue-700">{message}</p>}</div></div></main>}
